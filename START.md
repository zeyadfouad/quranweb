# كيفية تشغيل الموقع تلقائياً

## 🚀 التشغيل السريع (بدون كتابة أوامر)

### الطريقة الأسهل:
**انقر نقراً مزدوجاً على ملف `start-all.bat`** 

سيقوم الملف تلقائياً بـ:
- ✅ التحقق من Python
- ✅ تثبيت المكتبات المطلوبة (إن لم تكن مثبتة)
- ✅ بدء قاعدة البيانات و Backend Python
- ✅ بدء Frontend

---

## 📋 التشغيل اليدوي (اختياري)

إذا أردت التشغيل يدوياً:

```bash
# 1. تثبيت مكتبة concurrently (مرة واحدة فقط)
cd vite-project
npm install

# 2. تشغيل كل شيء
npm run dev:all
```

---

## 🎯 روابط الخوادم:

- **Backend (قاعدة البيانات)**: http://localhost:5001
- **Frontend (الموقع)**: http://localhost:5173

---

## ⚠️ ملاحظات:

- تأكد من تثبيت Python قبل التشغيل
- عند الضغط على `Ctrl+C`، سيتم إيقاف جميع الخوادم معاً
- قاعدة البيانات SQLite ستنشأ تلقائياً في `backend_py/users.db`

